# Báo cáo QA nghiêm khắc — Chạy Đua Cùng Hana

**Ngày audit:** 23/08/2026  
**Phạm vi:** UI/đồ họa, responsive, gameplay, input, bảng Top 20, asset, âm thanh, độ tin cậy build và hai bản công khai.  
**Nguyên tắc:** Đợt này chỉ đánh giá và lập danh sách xử lý. Không tự sửa mã game hoặc ghi dữ liệu kiểm thử vào leaderboard thật.

> **Kết luận:** Game đã có nền tảng chức năng khá tốt, hai bản công khai tải được, Top 20 và luồng chọn nhân vật hoạt động. Tuy nhiên, lỗi người dùng báo là **có thật và có thể tái hiện**: ở nhóm viewport mobile trung bình, chân dung mascot rộng hơn cột hình nên đè lên tên. Đây là lỗi **P0 về trình bày**, cần sửa trước đợt phát hành UX tiếp theo.

## 1. Phương pháp và phạm vi bằng chứng

| Nhóm kiểm tra | Cách kiểm tra | Kết quả tóm tắt |
| --- | --- | --- |
| Responsive/đồ họa | Chụp các trạng thái landing, setup, hướng dẫn, demo, kết quả, Top 20 ở 360×780, 412×915 và 1280×720. | Tái hiện được lỗi chồng chân dung–chữ ở setup mobile; các màn khác không có lỗi chặn chơi rõ ràng trong ảnh QA. |
| Gameplay & input | Chạy demo công khai, đọc test GameWorld/HUD/spawn, kiểm tra lệnh làn–nhảy–trượt. | Lượt demo render canvas/hazard/HUD; 40 regression tests đạt. |
| Chọn nhân vật | Đi landing → setup trên GitHub Pages, chọn Pompompurin. | Ảnh, chỉ số, tagline, trạng thái chọn và CTA đổi đúng. |
| Leaderboard | Mở Top 20 trên GitHub Pages với dữ liệu thật; kiểm tra danh sách cuộn. | Đủ 20 hạng, dữ liệu đọc được, danh sách cuộn nội bộ. |
| Âm thanh | Chạm công tắc nhạc hai chiều; kiểm tra asset qua HTTP và test luồng gọi effect. | Nút đổi trạng thái đúng; 8/8 audio asset trả `200 audio/mpeg`; chưa đánh giá được chất lượng nghe thực tế bằng tự động hóa. |
| Độ tin cậy | Chạy `pnpm test`, `pnpm check`, `pnpm build`; lọc log runtime QA mới. | 40/40 test đạt, TypeScript/build đạt; không có 4xx/5xx hoặc console error mới trong khung audit 06:00+. |

## 2. Kết quả tổng quan

| Mảng | Trạng thái | Nhận định QA |
| --- | --- | --- |
| Khởi chạy, landing và setup | **Có điều kiện** | Luồng đi vào setup và chọn nhân vật hoạt động; layout thẻ nhân vật mobile có lỗi P0. |
| Gameplay/render | **Đạt chức năng** | Canvas, nhân vật, ba làn, vật cản, HUD và toast hiển thị trên Manus/GitHub Pages. |
| Điều khiển | **Đạt theo regression** | Test bao phủ phím/lệnh trái–nhảy–phải–trượt; cần thêm kiểm thử chạm thiết bị thật. |
| Leaderboard | **Đạt chức năng** | Top 20 thật, danh sách cuộn, dữ liệu mới sắp theo điểm. |
| Âm thanh | **Có điều kiện** | Asset và toggle/UI đúng; còn thiếu đánh giá nghe thực tế về loudness, cân bằng và độ trễ. |
| Hiệu năng/mobile | **Có rủi ro** | Bundle Babylon vẫn lớn; CSS có nhiều override chồng nhau ở responsive. |
| Khả năng tiếp cận | **Chưa đủ bằng chứng** | Chưa có audit keyboard-focus, screen reader, vùng chạm và giảm chuyển động trên thiết bị thật. |

## 3. Danh sách lỗi bắt buộc xử lý

| ID | Mức độ | Lỗi / tác động | Bằng chứng và nguyên nhân kỹ thuật | Khuyến nghị nghiệm thu |
| --- | --- | --- | --- | --- |
| **UI-001** | **P0 — Sửa trước phát hành UX** | **Chân dung mascot đè tên trong lưới chọn nhân vật** ở các điện thoại khoảng 391–560px rộng. Người dùng khó đọc tên và cảm giác UI bị vỡ. | Tái hiện tại **412×915**. Trong CSS, breakpoint mobile đặt card thành `grid-template-columns: 34px minmax(0,1fr)` nhưng override nằm sau đó lại ép `.portrait-disc` rộng/cao **45px**. Hình rộng hơn cột 34px nên lấn sang nhãn. | Trong breakpoint 391–560px, buộc avatar `max-width: 31–34px; width: 31–34px; height: 31–34px; flex: 0 0 31–34px`; hoặc tăng cột ảnh thành 45px. Thêm visual regression 393×852 và 412×915 xác nhận mép phải ảnh luôn trước mép trái nhãn. |
| **UI-002** | **P1 — Sửa ngay sau P0** | Có **hai đường render chân dung** trong cùng card: ảnh `<img>` origin-safe và CSS `background-image` theo `nth-child`. Hệ CSS vẫn dùng path `/manus-storage/...` tương đối. | CSS thiết lập nền chân dung 8 card bằng URL tương đối trong khi component đã có resolver absolute; nền này thừa, tạo cảnh báo Vite và rủi ro Pages/cache khi `<img>` lỗi. | Bỏ toàn bộ `nth-child background-image`; chỉ giữ `<img>` đi qua `assetUrl()` và fallback biểu tượng. Điều này giảm quy tắc chồng chéo, loại bỏ nguồn lỗi asset thứ hai. |
| **UI-003** | **P1 — Trải nghiệm/nhận diện** | **Ngôn ngữ thị giác runtime không đồng nhất với landing.** Landing là đường mây cream, pastel và sticker; gameplay là dải hồng phẳng rất bão hòa, viền vàng/vạch xanh kiểu arcade. | Quan sát trên Manus và GitHub Pages `?demo=1`; reviewer trực quan độc lập cũng nhận thấy sự chuyển cảnh làm giảm cảm giác “đường chạy mây” cao cấp. | Giữ độ rõ gameplay nhưng đổi road thành cloud-ribbon: mặt vanilla/kem, lane blueberry nhẹ, sao vàng làm marker, màu sherbet chỉ dùng cho reward/hazard. Đưa biểu tượng ngôi sao–đuôi mây vào HUD, result và Top 20. |
| **UI-004** | **P2 — Accessibility/khả năng đọc** | Chữ tên nhân vật và metadata ở một số breakpoint còn **8–10px**; vùng thẻ compact có nguy cơ khó chạm và khó đọc với trẻ em/người dùng điện thoại. | CSS dùng nhãn 8px ở mobile, 9px desktop; card 42–46px cao ở chế độ dày. | Đặt sàn typography 11px cho chữ chức năng, 12px cho tên nhân vật; đảm bảo target chạm tối thiểu 44×44px và đo lại trên 360×780/412×915. |
| **PERF-001** | **P2 — Hiệu năng tải đầu** | Bundle production còn nặng: chunk Babylon khoảng **1,70MB** raw, gzip khoảng **398KB**; vendor khoảng **610KB** raw. Người dùng mạng di động yếu có thể chờ landing/gameplay lâu. | `pnpm build` cảnh báo chunk trên 500KB. Babylon được tải cùng ứng dụng thay vì chỉ lúc vào gameplay. | Lazy-load GameCanvas/Babylon sau khi người chơi bấm “Bắt đầu hành trình” hoặc prefetch có điều kiện; đo LCP, thời gian tương tác và FPS trên thiết bị Android tầm trung. |
| **SEC-001** | **P1 — Vệ sinh log phát triển** | Log network debug lịch sử có thể giữ header xác thực thô trong môi trường dev. Dù không phải lỗi hiển thị công khai hiện tại, đây là rủi ro nếu file log được chia sẻ. | Audit log phát hiện request lịch sử có header nhạy cảm; **không ghi lại giá trị** trong báo cáo này. Các 404 `leaderboard.top30` cùng lúc thuộc phiên lịch sử trước khi chuyển Top 20, không tái diễn trong cửa sổ QA mới. | Mask tuyệt đối `Authorization`, cookie, token và body nhạy cảm ở debug collector. Giữ chỉ hostname, method, status, duration và trace ID đã lọc. |

## 4. Các điểm có thể cải thiện theo ưu tiên sản phẩm

| Ưu tiên | Hạng mục | Cải thiện cụ thể | Giá trị mang lại |
| --- | --- | --- | --- |
| P1 | Nhất quán mascot | Khi chọn nhân vật, đưa palette/phụ kiện/silhouette tương ứng vào runner 3D rõ hơn; hiện setup có chân dung phong phú hơn gameplay. | Người chơi thực sự cảm thấy lựa chọn 8 nhân vật có ý nghĩa. |
| P1 | Phản hồi thành tích | Khi vào Top 20, thêm huy chương/sao–mây nhỏ ở hạng #1–#3 và animation ngắn tôn trọng reduced motion. | Tăng cảm giác phần thưởng mà không che màn hình. |
| P1 | Âm thanh | Thêm menu “Âm nhạc / Hiệu ứng” tách riêng, lưu cấu hình; chuẩn hóa loudness và nghe thử trên loa Galaxy/iPhone. | Giảm trường hợp BGM che SFX hoặc SFX quá nhỏ. |
| P2 | Onboarding | Mỗi trang hướng dẫn nên có một khung “Hãy làm gì ngay?” cố định: **Đổi làn để lấy**, **nhảy vật thấp**, **trượt cổng cao**. | Rút thời gian hiểu luật, đặc biệt với trẻ em. |
| P2 | Top 20 | Làm rõ cue cuộn bằng gradient/fade và hiển thị “17/20” khi auto-scroll đến hạng mới. | Người chơi biết bảng có nội dung phía dưới và hiểu vị trí của mình. |
| P2 | Khả năng tiếp cận | Bổ sung focus ring thật, `aria-label` cho toàn bộ icon control, chế độ tương phản cao và QA bằng bàn phím/screen reader. | Hỗ trợ nhiều nhóm người chơi hơn và giảm lỗi thao tác. |
| P3 | Hiệu năng | Telemetry nhẹ cho Web Vitals/FPS, ảnh background responsive và preload asset theo bước chơi. | Dữ liệu thật để cân bằng đẹp mắt với tốc độ tải. |

## 5. Kết quả chức năng đã xác minh

| Hạng mục | Kết quả |
| --- | --- |
| Landing → setup | Đạt. Nút “Bắt đầu hành trình” mở setup. |
| Chọn nhân vật | Đạt. Chọn Pompompurin đổi ảnh, tên, tagline, chỉ số, trạng thái chọn và CTA. |
| Chặn tên trống | Đạt. CTA vẫn khóa khi tên chưa hợp lệ; không tạo điểm thật trong QA. |
| Gameplay demo | Đạt ở Manus/GitHub Pages. HUD hiển thị nhân vật, điểm, quãng đường/cấp, tốc độ, âm thanh/tạm dừng và toast. |
| Top 20 | Đạt. Hiện 20 hàng thật, xếp điểm giảm dần, có vùng cuộn nội bộ. |
| Logic hạng mới | Đạt theo UI regression: dòng mới được tô sáng, tự cuộn và có test hạng #17. |
| Âm thanh kỹ thuật | Đạt có điều kiện. Toggle hai chiều; 8/8 MP3 trả `200 audio/mpeg`; GameWorld test gọi đúng jump/pickup/clear/gameover. |
| Regression | Đạt: **40/40 tests**, `pnpm check` và `pnpm build`. |

## 6. Giới hạn của đợt audit

Không tạo lượt chơi thật hoặc gửi tên/điểm thử vào leaderboard công khai. Âm thanh không thể được đánh giá bằng tai qua automation, vì vậy các chỉ số **có thể tải, chuyển trạng thái và gọi sự kiện** đã đạt, nhưng đánh giá cảm nhận (âm lượng, clipping, latency, cân bằng nhạc–effect) vẫn cần một vòng nghe trên iPhone/Android có loa thật. Test unit cũng không thay thế được test chạm đa điểm, low-power mode và mạng 3G/4G thật.

## 7. Kế hoạch khắc phục đề xuất

1. **Sprint 1 — P0:** sửa `UI-001`, thêm screenshot regression 393×852 và 412×915, kiểm tra lại trên Galaxy S22/thiết bị tương đương.
2. **Sprint 2 — P1:** xoá CSS chân dung dư thừa, thống nhất visual runtime–landing và nâng độ rõ mascot trong scene.
3. **Sprint 3 — Chất lượng:** tách âm nhạc/SFX, audit accessibility, lazy-load Babylon và đo hiệu năng điện thoại thật.

## 8. Trạng thái quyết định QA

> **Không nên coi UI hiện tại là hoàn thiện trên mobile trung bình.** Có thể tiếp tục demo/chơi thử vì luồng core hoạt động, nhưng cần xử lý `UI-001` trước khi quảng bá hoặc coi màn chọn nhân vật đã đạt chuẩn chuyên nghiệp.

